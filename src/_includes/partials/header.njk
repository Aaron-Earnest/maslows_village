<header>
    <img class="site_logo" src="/assets/images/maslows_logo.png" alt="Maslows Village logo">
    <nav>
        <ul>
            {%- for entry in collections.all | eleventyNavigation -%}
            <li{% if entry.url == page.url %} class="active"{% endif %}>
                <a href="{{ entry.url }}">{{ entry.title }}</a>
            </li>
            {%- endfor -%}
        </ul>
    </nav>
</header>